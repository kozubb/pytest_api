# pytest_api

This repository contains API tests written with pytest and requests library.

---

## Continuous Integration

This project uses a **GitHub Actions pipeline** that runs the tests automatically on each push to the **main** branch.

You can view the test run results directly under the **Actions** tab in this repository.

---